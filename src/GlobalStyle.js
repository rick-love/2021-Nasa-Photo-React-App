import { createGlobalStyle } from 'styled-components';

export const GlobalStyle = createGlobalStyle`

:root {
    --nasa-blue: hsl(218, 86%, 31%);
    --nasa-orange:hsl(8, 97%, 56%);
    --box-shadow: 0px 5px 10px 0px rgba(164, 167, 168, 0.95);
}

`;
